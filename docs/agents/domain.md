# Domain Docs

This is a single-context repository.

Before exploring, read `CONTEXT.md` and any relevant ADRs under `docs/adr/` when they exist. Proceed silently when those files are absent.

Use terminology defined in `CONTEXT.md`. If work conflicts with an existing ADR, identify the conflict explicitly rather than silently overriding the decision.
